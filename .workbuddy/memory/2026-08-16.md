# 2026-08-16

## 流程分类功能设计拷问(grill-me 进行中)

用户需求:流程运维新增「流程分类」菜单,页面分页展示 bpm_process_category;流程设计器基础设置 tab 增加「流程类型」下拉,发布时存入 t_bpmn_conf.bpmn_type。

已确认决策:
- `t_bpmn_conf.bpmn_type` 存 `bpm_process_category.id`;存量数据为空、无需清洗
- 设计器下拉:只过滤 `is_del=0`,**不过滤内置 id 1/2**,不过滤 is_app
- 现状事实:表/实体/Mapper/Service 已存在但仅 App 侧使用;无 PC 端 Controller;`/bpmnConf/edit` 当前不写 bpmn_type;`BasicSetting` 有被注释的"流程分组"下拉(绑 bpmnType);菜单为 RuoYi sys_menu DB 驱动,仓库无 SQL
- 坑:`effectiveBpmnConf` 启用版本时用旧有效版本 bpmnType 覆盖新版本;`DIC_LCLB` 字典旧语义

分支 2-5 决策:
- 分类管理页:完整 CRUD(新增/编辑/上移/下移),**删除按钮保留但点击提示"演示环境,禁止删除数据"**,不真正删除;菜单改 `antflow-vue/public/mock/menu.json`(flowDevOps 流程运维下加子菜单),非 SQL
- 设计器下拉:LF/DIY 共用 `BasicSetting` 组件改一处;选填、无默认值;选项只显示 `process_type_name`
- 发布链路:`/bpmnConf/edit` 后端天然透传 bpmnType(BeanUtils.copyProperties+insert),零改动;前端 BasicSetting 需在 onMounted 补 bpmnType 回显
- 修 `effectiveBpmnConf`(BpmnConfBizServiceImpl:941):去掉 `.bpmnType(confInDb.getBpmnType())` 继承,启用以新版本为准;appId/isAll 继承保留
- 后端需新增:PC 端分类 Controller(CRUD + 分页查询 + 下拉 options 接口,复用 BpmProcessCategoryService)

待实现清单:Controller + 分页查询、menu.json 子菜单 + 前端分类页、BasicSetting 下拉 + 回显、effectiveBpmnConf 删一行。

## 已实现(2026-08-16)

- 设计文档落盘 `.scratch/流程分类功能设计.md`
- 后端新增 `BpmProcessCategoryController`(`/processCategory`):POST /listPage(分页)、POST /save(复用 editProcessCategory)、GET /operation/{type}/{id}(2上移 3下移 4删除)、GET /options(下拉选项,is_del=0 不过滤 id/is_app)
- `BpmProcessCategoryService.selectPage` 新分页方法(MP Page + LambdaQueryWrapper,sort asc);注意 `.like(condition, col, val)` 参数会先求值,需先判空再 trim
- 新增 DTO `BpmProcessCategoryPageReq`(pageDto + processTypeName)
- 修 `BpmnConfBizServiceImpl.effectiveBpmnConf`:去掉 bpmnType 旧值继承(保留 appId/isAll)
- 前端新增 `api/workflow/processCategoryApi.js` + `views/workflow/flowDevOps/processCategory/index.vue`(CRUD 增改/上移下移,删除点击提示"演示环境,禁止删除数据"不发请求;新增可选手 PC/APP,编辑禁改 isApp)
- `public/mock/menu.json` flowDevOps 下加 processCategory 子菜单(流程分类)
- `BasicSetting/index.vue`:注释掉的"流程分组"改为「流程类型」下拉(clearable filterable,绑 form.bpmnType,options 来自 /processCategory/options);bpmnType 默认 undefined;编辑回显 `form.bpmnType = basicData.bpmnType`;LF/DIY 共用生效
- 编译环境备忘:mvnw wrapper 缺 jar;git-bash 调 mvn 报 classworlds 错 → 用 `java -cp <maven>/boot/plexus-classworlds-2.6.0.jar -Dclassworlds.conf=<maven>/bin/m2.conf -Dmaven.home=<maven> -Dmaven.multiModuleProjectDirectory=<proj> org.codehaus.classworlds.Launcher ...` 绕过;注意 main class 是 **org.codehaus.classworlds.Launcher**(不是 plexus),路径必须用 `C:/` 显式 Windows 风格(git-bash 的 `/c/` 参数不会自动转换);项目 JDK 8(Spring Boot 2.7.17)

## 后续调整(用户反馈)

- 分类页去掉「使用端」选项(新增/编辑弹窗只留分类名称)和「是否App」列;columns 同步为 3 列;前端 form 不再传 isApp,后端 controller 已有 isApp==null→0(PC)兜底,新增分类恒为 PC 端
- 流程类型下拉确认已在 BasicSetting(LF/DIY 设计器基础设置),用户验证 OK
- 前端构建:首次 vite build 2519 模块转换成功,但写 dist 时 EPERM(旧产物占用);用户明确后续不构建、自行验证

## 发起流程页设计(grill-me 定稿)

需求:任务中心(/flowtask)菜单下加「发起流程」;页面 = 全部可用流程(同流程中心 Workplace 逻辑),按流程分类(bpm_process_category)分组,不再分 DIY/LF/outside;分类按 sort 排序;左中右三栏流式布局。

最终决策:
- 范围:DIY+LF+outside 全纳入;bpmn_type 空(存量+outside)→「未分类」组放最后
- 数据:后端聚合分页接口;可用统一口径 = t_bpmn_conf effective_status=1(实现时核查 DIY/outside 是否在 t_bpmn_conf)
- 布局(我拍板):每栏 8 卡片位(标题占 1 位);超长分类块不跨栏块内滚动;一页=最多 3 栏;服务端按栏切分,分类不跨栏不跨页
- 排序:分类 sort asc;分类内创建时间 asc;未分类最后
- 权限:is_all 保持原语义(1=全员),用户自行刷所有数据 is_all=1;is_all=1 直接放行,is_all=0 按 bpm_process_permissions 创建权限(permissions_type=2)过滤;匹配 = 当前用户id(object_type=1)∪用户部门id(object_type=2)∪用户角色id(object_type=3),从主体出发不展开部门全量
- AfUserService 需新增 getUserRolesById/getUserDepartmentsById → List<BaseIdTranStruVo>;角色走 t_user_role,部门关联表待确认
- 菜单:任务中心第一位;点击沿用流程中心(DIY/LF→/startFlow/index,outside→/startOutside/index)
- 权限表 process_key = 流程 formCode(待实现确认)

## 发起流程页已实现(2026-08-16)

- 设计文档 `.scratch/发起流程页设计.md`
- 后端:
  - `AfUserService`+`UserServiceImpl` 新增 `getUserRolesById`(RoleMapper.queryRoleIdsByUserId + queryRoleByIds)、`getUserDepartmentsById`(UserMapper.queryDepartmentByUserId)
  - `UserMapper.xml` 新增 `queryDepartmentByUserId`(t_user.department_id + t_department.path 前缀查子部门,path 空退化为仅本部门)
  - `BpmnConfMapper.selectStartFlowList`(t_bpmn_conf effective=1 LEFT JOIN bpm_process_app_application 取 applicationId)
  - 新增 `StartFlowListBizServiceImpl`(/startFlowList/page):聚合(conf 有效流程 + 未设计 DIY 适配器 Bean 并入未分类)→ 权限过滤(is_all=1 放行;is_all=0 按创建权限,从用户主体出发:用户id∪部门含子部门∪角色)→ 按分类 sort asc 分组(未分类最后,分类内 createTime asc)→ 栏切分(每栏 8 卡片位标题占 1,超长块整栏内滚,一页最多 3 栏,块不跨栏不跨页)
  - 新增 `StartFlowListController`、VO `StartFlowListRowVo`/`StartFlowCategoryVo`
- 前端:
  - `api/workflow/startFlowListApi.js`
  - `views/workflow/flowTask/startFlowList/index.vue`:三栏 flex,分类块(标题+数量+卡片),超长块内滚(max-height 480px),上一页/下一页+第 X/Y 页;点击 DIY/LF→/startFlow/index,OUTSIDE→/startOutside/index
  - `menu.json` flowtask children 第一位加「发起流程」(path startFlowList)
- 已知坑:`BpmProcessPermissionsBizServiceImpl.getProcessKey` 的部门参数传 userId 但 `ListSubDepartmentByEmployeeId` 是按部门 id 查,部门权限实际失效——本次未动它,新逻辑独立实现;后续可回头修

## 权限模式调整(object_type=4 全员,去 is_all)

- 用户决策:发起权限校验**去掉 `t_bpmn_conf.is_all`**;`bpm_process_permissions.object_type` 增加 **4=全员**,全员时 permissions_type 仅 2(创建),object_id 固定 "ALL";流程权限管理页同步调整;存量由用户自行配全员-创建权限
- 后端改动:`ProcessPermissionsBizServiceImpl.save`(objectType 1/2/3/4,全员校验 permissionsTypes 仅 [2]、强制 objectId="ALL")、listPage(加 object_type=4 过滤)、buildListVos(objectName 显示"全员");`StartFlowListBizServiceImpl` 权限过滤去掉 is_all,全员走 object_type=4;`BpmnConfMapper.selectStartFlowList` 删 is_all 列、`StartFlowListRowVo` 删 isAll 字段
- 前端改动:`processPermissions/index.vue` 对象类型加「全员」,全员隐藏对象选择、权限类型仅创建可选、查询区对象显示"全员"、提交 objectIds=["ALL"]

## 发起流程页条件过滤 + 管理员(2026-08-16)

- 过滤:流程类型(下拉 filterable 选中=id 等于,含「未分类」value=-1→IS NULL)、formCode(like)、流程名称(like);**优先级 名称>formCode>类型**,后端 `StartFlowListBizServiceImpl.filterByQuery` 兜底;过滤后仍按分类分组;条件变化回第 1 页;空结果 el-empty
- 管理员:注入 `BpmnProcessAdminProvider`(@Autowired(required=false)),`provideProcessAdminInfo().id` == 当前用户 → 跳过权限过滤展示全部(实现 ProcessAddminProvider 默认 id=20 任盈盈,可 @Primary 改写)
- 接口 `POST /startFlowList/page` body `{page,bpmnName,formCode,categoryId}`;新增 DTO `StartFlowListPageReq`;前端搜索区三控件 + 提示 + 分类 options(processCategoryApi)

## 版本比较功能设计(grill-me 定稿, 2026-08-16)

需求:流程管理-流程类型列表[更多]菜单加[版本比较],比较同流程两个版本模板配置差异。

**关键源码事实**:
- `t_bpmn_node.node_id`(String 列,独立于自增主键):前端 NodeUtils.idGenerator() 生成,后端 edit() BeanUtils 透传 → **拷贝链路下跨版本稳定**;detail/{id} 返回的 nodeId 即此列值。每次保存全量 insert 新 conf+node 行,主键必变,node_id 不变。
- 版本列表:POST /bpmnConf/listPage(body 带 formCode);详情:GET /bpmnConf/detail/{id} → BpmnConfVo(nodes+lfFormData+confConfigJson+extraFlags)。
- 入口:flowCategory/index.vue L79-88(LF)/L153-162(DIY) el-dropdown;流程图自研(Process/index.vue 或 HorizontalDesigner);approverDrawer 10 个 tab;表单 VForm widgetList,options.name 唯一。

**定稿决策**:
1. 范围:同 formCode 两版本(含未激活);下拉数据=listPage 带 formCode
2. 形态:全屏 dialog(~90% 视口),顶部两版本下拉+互换,选版即自动比较;默认 源=活跃版,目标=最新版(同版取次新)
3. 引擎:**纯前端 diff**,两次 detail/{id},后端零改动
4. 节点对齐:①nodeId 相同→配对(主) ②未配对但前驱/父已配对+名称相同+类型相同→仍算同一节点(位置锚定兜底,处理删了重建) ③仍未配对→新增/删除 ④配对后比名称/位置/属性差异
5. 展示:三 tab+差异计数徽标(设置/表单/节点);节点 tab 双图并排(左源右目标,绿新增红删除橙修改,点击任一侧节点→右侧固定差异区);节点属性差异区=原 drawer 10 tab 降为 10 section 单页,**只列差异字段**(源值→目标值,无差异 section 折叠);表单差异=按 widget options.name 对齐,仅比 label/type/required+新增/删除,多表单按表单名对齐,DIY 占位;设置=总结式全量罗列+差异高亮
6. 边界:仅1版本按钮禁用+tooltip;无差异绿色空态;LF/DIY 双入口;节点 tab 加「下一处差异」循环跳转

待实现:前端为主(新组件 versionCompareDialog + diff 引擎工具类),flowCategory 两处菜单项。

## 版本比较已实现(2026-08-16)

- 设计文档 `.scratch/版本比较功能设计.md`;逻辑测试 `.scratch/tmp/test_versionCompare.mjs`(esbuild bundle + node 断言跑通,模拟 拷贝/改名/加签变更/增删节点/删了重建/条件改/表单增删改/设置变更)
- 新增文件:
  - `antflow-vue/src/utils/antflow/versionCompare.js`:diff 引擎。alignTrees(nodeId 主匹配→位置锚定兜底→增删)、diffNodePair(11 个逻辑 section 分组,显式字段+递归 diff+VOLATILE_KEYS 过滤+行去重)、位置变更用"沿链回溯最近已配对锚点"的结构签名(免疫前驱改名/中途插入节点误报)、diffForms(widget options.name 对齐,仅 label/type/required+增删)、diffSettings(extraFlags 按位拆解)
  - `views/workflow/components/versionCompare/index.vue`:全屏 dialog(顶部双下拉+互换+自动比较,三 tab 带差异徽标,节点 tab=双图+右侧 400px 差异区+下一处差异循环跳转)
  - `compareTree.vue`(单侧图容器,scrollIntoView 定位) + `cmpNode.vue`(递归只读渲染器,自研 CSS,绿增/红删/橙改)
- 改动 `flowCategory/index.vue`:LF/DIY 两处[更多]菜单加 versionCompare 项,handleMoreCommand 分发打开 dialog
- 关键坑:①`FormatDisplayUtils.getToTree` 会 mutate 入参,比较前必须 JSON 深拷贝;②条件节点(nodeType 3)的 conditionList 在 property 里,须提供 property.sort 否则转树报错;③formatdisplay 给节点顶层镜像了 signType/setType/nodeApproveList 等冗余字段,与 property.* 双报需去重;④flattenTree 父循环+walk 入口曾双 push,对齐全乱——测试抓出
- 后端零改动
- 默认选版调整(用户要求):目标=当前活跃版本(effectiveStatus==1),源=活跃版的上一个更早版本(版本列表按 id desc,取 activeIdx+1);活跃版已是最早版则取更新的一个;无活跃版退化为 最新 vs 次新
- 表单差异表第一列改显示字段可读 label(diffForms 字段对象补 label,增删取 widget options.label、修改取 tl||sl;表格头「字段唯一名称」→「字段」,hover title 仍显示唯一 name)

## 流程节点「抗去重」功能设计(grill-me 定稿,2026-08-16)

需求:流程设计-节点高级设置加「抗去重」勾选,排除当前节点参与审批人去重;流程发起时自动生效。

关键事实(代码核实):
- `BpmnNodeVo.deduplicationExclude`(boolean)与 `BpmnNode.deduplicationExclude` 列**已存在**;主去重 `BpmnDeduplicationFormatImpl.singlePlayerNodeDeduplication(L228)/multiPlayerNodeDeduplication(L268)` 已认该标志(early-return = 双向全排:本节点不标重、审批人不进 approverList)
- 两条读路径都靠 `BeanUtils.copyProperties(bpmnNode, bpmnNodeVo)` 拷贝该列:老路径 `getBpmnNodeVo`(BpmnConfBizServiceImpl:1851)、JSON 路径 `getBpmnNodeVoFromJson`(:1915)——写列即可自动生效
- 现状不一致:老读路径对 `copyNodeV2`/`automaticNode` 标签强制 setDeduplicationExclude(true);**JSON 读路径只处理 copyNodeV2,不处理 automaticNode/conditionApproveNode/conditionCopyNode** → 自动/条件节点在 JSON 流程里会被去重
- 自选去重 `BpmnOptionalDuplicatesImpl`(antflow-common)不认 deduplicationExclude → 勾了也静默失效
- 前端:仅 approverDrawer 有「高级设置」页签;条件审批(12)走 approverDrawer,条件抄送(13)走 copyerDrawerV2(无高级设置页签),自动节点(9)走 autoNodeDrawer(单区块);formatdisplay/formatcommit 原地 Object.assign 改字段,新字段透传无损;nodeUtils.js 节点默认值需补字段

最终决策:
- 语义 = 全排(复用现有标志,不改 dedup 核心);相邻去重(SKIP_NEXT)副作用「B抗去重导致C被去重」接受,加提示文案不写代码
- 落点 = **DB 列直写**(非 JSON):保存时 edit 循环两处 updateById 都补 `updateNode.setDeduplicationExclude(bpmnNodeVo.isDeduplicationExclude())`(L272-275 无适配器分支 + L296-300 正常分支);JSON 里不存
- 范围 = 审批人节点(默认关、可开关)+ 自动节点(9)/自动办理(11)/条件审批(12)/条件抄送(13)(默认开、**只读常开**)
- 服务端强制:JSON 读路径补 automaticNode/conditionApproveNode/conditionCopyNode 三个标签的 setDeduplicationExclude(true),与老路径对齐(顺带修不一致)
- 自选模块:改 `BpmnOptionalDuplicatesImpl`,遍历时跳过 isDeduplicationExclude 节点 + 处理自选节点前判标志
- 流程级「不去重」(deduplicationType=1)时勾选框照常显示,不做特殊处理
- 前端:approverDrawer 高级设置加 switch;copyerDrawerV2 补高级设置页签(条件抄送只读常开);autoNodeDrawer 直接放只读开关;nodeUtils.js 审批节点默认 false、自动/条件节点默认 true
- 回归留意:复制流程/模板导入克隆 BpmnNode 行的路径不在 edit 循环,列不跟着拷

## 抗去重功能实现 + 合并到 v-next(2026-08-16)

- 已实现(dev 分支):后端 edit 循环两处落列、JSON/老读路径三标签强制排除、common 自选去重认标志;前端 nodeUtils 默认值、copyerDrawerV2 高级设置页签、autoNodeDrawer 只读开关;antflow-common 编译+install 通过,antflow-engine 编译通过
- **合并冲突解决(v-next-breaking-dynamic-v3.1 分支,merge dev)**:v-next 已重构 approverDrawer(高级设置=AdvancedStepPanel 组件;autoNodeDrawer=条件/动作设置页签;新增 nodeType 18/19 前进/退回)。冲突解法:两 drawer 取 ours(v-next 结构),抗去重开关改放 `drawer/panel/AdvancedStepPanel.vue`(新增 isAntiDedupLocked 计算属性:nodeType12/property12/15 锁死),autoNodeDrawer 在 drawer 模式页签后加只读块;dev 自动合并进转办设置 tab 的错位抗去重块已随 --ours 丢弃;memory 冲突取并集
- 编译验证:jimuoffice antflow-engine -pl -am 编译通过(用修正后的 classworlds 命令)
- ⚠️ 部署注意:**antflow-common 源码版本 2.0.0,但 antflow-engine pom 依赖 2.0.0-m8(本地 .m2 中的旧构建)**;已 install 新的 2.0.0 到本地仓库,但 engine 依赖版本需用户决策对齐(m8 里没有自选去重修复)

## 抗去重扩展:v-next 全部自动/条件语义节点(2026-08-16, v-next-breaking-dynamic-v3.1)

- 用户要求:v-next 节点类型远多于 dev,凡名字带[自动]/[条件]的都有自动语义,全部要服务端强制抗去重;后端不构建(用户自行构建),只 lint
- v-next 节点清单(nodeUtils.js create 函数):9自动/11自动办理/12条件审批家族(条件审批/条件推进/条件完成/条件拒绝/条件自动加批/条件自动转办)/13条件抄送/17协助(手动,不抗)/18自动推进(含自动完成子型)/19自动退回/20条件退回/21条件退回发起人
- 关键事实:①保存时 nodeSpecialProcess 先清空 labelList,所有标签由 BpmnConfBizServiceImpl 保存循环按 VO 标志重贴,12 家族子类型**只贴各自子类型标签**(如 condition_advance_node),不贴 condition_approve_node → 读路径只查 3 个旧标签覆盖不到;②BpmnNode 实体**没有** isAuto*/isCondition* 标志列,读路径 VO 标志只能靠标签判断;③drawer 分发:4/12/17/18/19/20/21→approverDrawer,8/13→copyerDrawerV2,9→autoNodeDrawer;approverDrawer 的高级设置页签对 18/19 隐藏
- 改动:
  - `NodeUtil`(antflow-base):新增 `AUTO_CONDITION_NODE_LABELS`(13 个标签)+ `isAutoConditionNode(List<BpmnNodeLabelVO>)` 工具方法
  - `BpmnConfBizServiceImpl` 两条读路径(getBpmnNodeVo 老路径 / getBpmnNodeVoFromJson JSON 路径):labelList 构建后加 `if (NodeUtil.isAutoConditionNode(labelVOList)) setDeduplicationExclude(true)`
  - 前端 `AdvancedStepPanel.vue`:isAntiDedupLocked 扩展 nodeType 20/21(条件退回/条件退回发起人);12 家族已由 nodeType===12 覆盖
- lint:git diff --check 干净;13 个 StringConstants 常量全部存在;NodeUtil 经 `import org.openoa.base.util.*`(L32)可见;未构建(用户自行构建)
