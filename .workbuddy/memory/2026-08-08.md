# 2026-08-08

## 横向传统风格流程设计器(grill-me 澄清 + 首版实现)

**需求澄清(10 项,全部经周老板确认):**
1. 数据模型:横向/竖向共用同一棵树(nodeUtils + FormatCommitUtils/FormatDisplayUtils),横向只换渲染
2. 换行:每排统一左→右,满宽后折线从上排末连下排首(宽度自适应,非固定列数)
3. 网关视觉:菱形 + BPMN 符号(排它×/并行+/包容○)+ 配色(条件分支橙/动态条件紫/并行审批蓝/条件并行绿)
4. 分支链:水平泳道 + 自动汇合,分支区段整体参与换行
5. 拖线:只能新建节点(不连已有节点),菜单全量复用现有 29 项+克隆器;网关类选完直接出菱形+默认分支
6. 右侧面板:常驻 380px 可折叠,复用现有 drawer 内容组件(embed 模式)
7. 切换:基础设置勾选(extraFlags 256 位)→ 流程设计 tab 默认横向;工具栏/导航可横竖切换
8. 分支增删:网关悬停"+" + 面板管理
9. 视觉:延续现有节点风格(圆角方框+彩色标题条+内容区),连线改 BPMN 直角折线+箭头
10. 布局:自动布局,节点本体不可拖动

**实现文件:**
- 后端:`antflow-base/.../constant/enums/BpmnConfFlagsEnum.java` 加 `TRADITIONAL_DESIGNER(0b100000000=256)`
- 前端新增 `src/components/Workflow/HorizontalDesigner/`:
  - `layout.js` — 纯 JS 布局引擎(树→坐标/边路径/满宽换行/泳道/汇合),已 node 单测通过
  - `index.vue` — 画布容器(SVG 渲染、选中联动 store、拖线、加分支、删除、缩放、校验 getData)
  - `nodeMenu.vue` — 拖线松手弹菜单(29 项+克隆器,复用 NodeUtils 工厂)
- 6 个 drawer 组件加 `embed` 内嵌模式(promoter/approver/copyer/copyerV2/condition/autoNode)
- `basicSetting/index.vue` 加"传统风格设计器(横向)"勾选(extraFlags 256 位读写)
- `flowDesign/lf.vue` 集成:读取 flag、isHorizontal 条件渲染 HorizontalDesigner/Process、导航栏横竖切换按钮
- 已自测:布局引擎样例(条件网关带分支链+并行网关+换行)几何正确;10 个 SFC 编译通过;store 字段核对

**待办/注意事项:**
- `diy.vue`(DIY 流程)尚未集成横向渲染(入口相同,做法同 lf.vue)
- 横向画布高度 calc(100vh-85px) 需真机调整;节点标题条圆角与节点描边衔接可微调
- 节点内文字按宽度截断(13px 估算),超长审批人列表可能显示不全
- 保存/反显链路零改动,横竖数据互通由"共用一棵树"保证

## 修复:勾选传统风格无效果(15:53)
- 根因:basicSetting 勾选"传统风格设计器"未发事件通知 lf.vue,useTraditionalDesigner 只在页面加载时从 extraFlags 解析一次
- 修复:
  1. `basicSetting/index.vue` defineEmits 加 `traditionalDesignerChange` + watch(useTraditionalDesigner) 发射
  2. `flowDesign/lf.vue` BasicSetting 绑 `@traditionalDesignerChange` → onTraditionalDesignerChange 同步 useTraditionalDesigner + viewMode='auto'
  3. `HorizontalDesigner/index.vue` 响应式修复:layout 普通对象 → layoutRef(ref),增删节点后 relayout 才能触发视图刷新(computed 全部基于 layoutRef.value)

## 修复:勾选时控制台 vnode.component is null(16:07)
- 现象:勾选"传统风格设计器"checkbox → Uncaught TypeError can't access property "subTree", vnode.component is null
- 根因:el-checkbox 事件链中同步替换大组件树(Process 含大量 Teleport drawer),与 checkbox 自身更新交错触发 Vue 边界 bug
- 修复(flowDesign/lf.vue):
  1. processDesign 容器 `v-if="nodeConfig && activeStep === 'processDesign'"`(基础设置页不挂载画布,勾选时不发生组件树替换)
  2. 两个设计器组件加显式 key
  3. `onTraditionalDesignerChange` 与 `toggleView` 均用 nextTick 延迟提交状态,把组件树切换移出事件链

## 修复:切到流程设计页 conditionDrawer 空引用(16:12)
- 现象:切 tab 报 `can't access property "conditionNodes", $setup.conditionsConfig is null`(_sfc_render conditionDrawer.vue)
- 根因:embed 分支用 v-show 常驻渲染(v-show 只是隐藏,模板照样求值),而 conditionsConfig 初始 null、originalConfigData 初始 {};原版在 el-drawer 内关闭时不渲染内容所以从未暴露
- 修复(conditionDrawer.vue,replace_all 同时修原版+embed 分支):
  - 优先级 select 加 `v-if="conditionsConfig && conditionsConfig.conditionNodes"`
  - `originalConfigData.conditionList.length > 1` → `(originalConfigData.conditionList || []).length > 1`
  - `nodeApproveList.length > 0` / `== 0` → `(originalConfigData.nodeApproveList || []).length`
  - 条件组间隔 `conditionList.length != conditionGroupIdx+1` → 加 `|| []`
- 已用脚本扫描全部 6 个 embed drawer 模板的 .length 裸引用:其余均安全(copyerDrawer 有 && 前置、promoter/autoNode/copyerV2 初始值非空或 undefined 安全)

## 修复:横向设计器 5 项体验问题(16:32)
1. 结束圆被画布切掉 → layout.js size.width 预留结束圆余量(maxRowW + PAD*2 + 56)
2. 节点标题/内容居中→居左(text-anchor start, x=n.x+14; 内容 x=n.x+14 宽减 36)
3. 节点内容区右侧加箭头 ❯(与竖向一致, x=n.x+w-16)
4. 右侧面板 380→480px(embed drawer 内容不再左右滑)
5. 切换节点自动保存:新增 flushCurrent()——切换/取消选中前把 store 对应 config 的 value(抽屉实时编辑对象,与抽屉内引用一致)合并回树(排除 childNode/conditionNodes/parallelNodes 保持树引用);openConfig/selectGateway/openCondition/clearSelect 开头调用;selected 增加 storeKey(approver/promoter/copyer/copyerV2/condition/autoNode)映射 CFG_MAP
   - 连带修复隐藏 bug:openCondition 数据源必须传**网关**(含 conditionNodes),不能传条件节点(否则 conditionDrawer 的 originalConfigData=undefined 崩溃);用 nodeMap 找条件的 parent
   - autoNodeDrawer 补 watch:groupRelation 实时同步回 autoNodeConfig1.value(否则自动保存丢失该字段)

## 修复:节点交互三项(16:42)
1. 双击节点标题改名:SVG dblclick → hd-canvas 内 absolute input(坐标 = 布局坐标×zoom - scrollLeft/Top),Enter/失焦提交 nodeName + relayout;esc 取消
2. 标题条左侧类型图标:svg sprite 全局注入(<use href="#icon-xxx">),nodeIcon(node) 映射与 nodeWrap 的 svg-icon 完全一致(approve/copy-user/assist/auto-finish/auto-drive-*/drive-back/conditional-drive-* 等);标题文字右移到 x+32
3. 审批人实时反馈到节点:desc() 改为与竖向 showText 一致——$func.setApproverStr/copyerStr/setCopyStrV2/autoNodeConditionStr 实时计算(不依赖面板保存才刷新的 nodeDisplayName);回写 watch 由"仅 flag"改为"任何变化"都合并(syncConfig,deep watch),面板选人后节点内容立即更新;条件编辑场景额外字段级合并 conditionNodes(保持引用,条件文字实时刷新);空值兜底显示"请配置"

## 修复:双击改名后切节点名字被覆盖(16:50)
- 现象:双击改名成功后,点击其它节点,名字变回默认"审批人"
- 根因:改名只改了树里的 nodeName,右侧面板里的编辑副本(打开面板时的深拷贝)仍是旧名;点击其它节点时 flushCurrent 用旧副本合并回树覆盖了新名
- 修复:commitRename 改名时同步更新 CFG_MAP 对应 store config 的 value.nodeName(sel.node === ed.node 时才同步),树与面板副本保持一致,切走时不再覆盖

## 修复:发布校验误报"未选择审批人"(16:55)
- 现象:节点已设审批人且反显正常,发布仍报【张三审批】未选择审批人
- 根因:校验依赖节点 error 字段,而横向自动保存/实时同步不刷新 error(竖版是点"确定"时 drawer 才刷新 error=!setApproverStr)
- 修复(index.vue):
  1. reErr 改为实时判断:审批人类[4,12,17,20,21] 用 !$func.setApproverStr;抄送[6] !copyerStr;[8,13] !setCopyStrV2;条件节点 condUnconfigured(conditionList 空);并行分支 parallelNodes 实时判断
  2. 新增 nodeHasError()(与校验同逻辑),isTried 后节点红框也实时判断,避免已配置节点被误标红

## 修复:发布校验仍误报(16:59)
- 上轮 reErr 保留了 `error ||` 短路:横向自动保存不刷新 error(仍为创建时 true),error|| 恒真,实时判断未生效
- 修复:reErr 彻底去掉 error 依赖(只实时判断);syncConfig/flushCurrent 合并后调用 refreshNodeError() 同步 error 字段(与实时校验一致),数据/序列化不依赖过期 error
- 教训:横向设计器下 error 字段不可信,一切以 $func 实时计算为准

## 修复:切换节点后面板不切换(17:05)
- 现象:点发起人再点审批人,右侧面板仍是发起人的
- 根因:openConfig 只打开新面板(setXxx(true)),没关闭旧面板——closeAllDrawers 只在 selectGateway 里调了,普通节点切换漏掉;embed 模式下多个 drawer 同时 visible 垂直堆叠,旧的盖在上面
- 修复:openConfig 和 openCondition 开头加 closeAllDrawers()
- 面板映射核对(与 nodeWrap.setNodeInfo 一致):type1→promoter, 4/12/17/18/19/20/21→approver, 6→copyer, 7→approver(带分支index), 8/13→copyerV2, 9→autoNode, 2/3→condition

## 修复:点击条件节点(分支)面板不弹出(17:35)
- 现象:添加条件分支后点条件节点,右侧面板无反应
- 根因:openConfig 对 type3 传 value=条件节点本身,而 conditionDrawer 约定 value=网关(经 value.conditionNodes[priorityLevel-1] 取当前条件)→ originalConfigData=undefined → watch 抛错,面板不渲染(与条件线标签同源 bug,此前只修了 openCondition)
- 修复:openConfig 的 type3 分支同样用 nodeMap 找条件所属网关,value 传网关、selected.node 改网关(保持高亮在条件节点)
- 顺带:条件节点标题图标按所属网关类型显示(condition/dynamic-condition/parallel-condition),与竖版一致

## 修复:删除条件分支行为与竖版对齐(17:38)
- 竖版 delConditionNodeTerm/delParallelNodeTerm:删到剩 1 个分支时,网关整体替换为"唯一分支的后续链"(条件分支连同剩余条件一起消失)
- 我的 delBranch bug:剩 1 个时替换成了分支节点本身(=only),而不是 its childNode → 条件(type3)残留主链,行为不一致
- 修复:对齐竖版——先唯一分支链尾接网关.childNode,再把父链中网关替换为 only.childNode(可为 null 即流程结束);条件/并行两个分支同样处理

## 修复:条件面板 4 项(17:45)
1. 条件1重影:desc(type3) 改为 $func.conditionStr(gw, idx) 实时算条件表达式,不再显示创建时默认名(与标题重影);conditionStr 需网关+索引(nodeMap 定位)
2. 优先级下拉:embed 分支去掉优先级 select(竖版虽可调但用户要求默认分配不可选),改为只读显示当前条件名(condition-idx span)
3. label/文本框错位:根因=竖版 scoped 样式挂在祖先类(.set_condition/.set_promoter/.set_copyer/.set_auto_node)下,embed 分支缺祖先类 → 样式全失效;已给 5 个 drawer 的 embed 根 div 补对应祖先类(approverDrawer 无祖先类依赖,保持)
4. 面板宽度:先恢复样式再看,仍挤则加到 520

## 修复:条件 label 与文本框分两行(17:49)
- 原因:竖版条件编辑器控件 350px + label 100px + 边距 ≈ 455px,480px 面板去掉内边距只剩 ~446px,差 9px 换行
- 修复:.hd-panel 480 → 520px(可用 ~486px,普通 input 条件一行放下;optType=6"介于"多控件在竖版 680 drawer 本就换行,属竖版原行为)

## 修复:条件节点脏/图标对齐/label 仍两行(17:52)
1. 条件节点显示逻辑重构:
   - 新增 layout.js condConfigured(node)(conditionList 是否有表达式);branchLabelText 仅已配置时显示,未配置线标签留空(消除"条件1"标签重影)
   - 新增 index.vue refreshCondDisplays(gw):syncConfig/flushCurrent 合并后刷新各条件 nodeDisplayName=conditionStr(与竖版 resetConditionNodesErr 一致)
   - desc(type3) 简化直接用 nodeDisplayName:未配置="请设置条件",已配置=条件表达式
   - 最终呈现:标题"条件1"、内容"请设置条件"、线无标签 → 干净
2. 图标顶对齐:use y+8→+7,标题文字 y+20→+15(与竖版顶端对齐观感一致)
3. label/input 仍两行:面板 520 仍不够(竖版控件 350+label 100=455 贴近边界),conditionDrawer 加 &.hd-embed-panel 覆盖样式(span 80px/控件 300px/input 200px/select 90px),保证一行
   - 再加一刀:用 flex 布局 + !important 强制覆盖(竖版 float/inline-block 在 scoped 嵌套覆盖下不可靠, !important 兜底保证 label 和输入框始终同行)

## 新增:网关区段聚合加号(18:02)
- 需求:条件分支汇合后无法继续加节点(竖向分支盒底部有加号)
- 实现:
  - layout.js placeGateway 给网关 entry 加 addBtn = {x: mergeX+EXIT, y: 区段中线-24}(汇合出口上方,常显)
  - index.vue 网关 g 内渲染聚合加号(circle+#),点击 openGwAddMenu:换算屏幕坐标弹 nodeMenu,dragFrom=网关节点 → 新节点 factory(gw.childNode) 后 gw.childNode=newNode,插入为汇合后第一节点(与竖向分支盒底部加号语义一致)
  - 条件/并行网关通用;与"选中网关加分支"按钮区分(菱形下方=加分支,区段右端=加后续节点)

## 修复:条件节点内容"请设置条件"叠加(18:08)
- 现象:conditionStr 把每个空组都拼"请设置条件" → 初始+加组后看到重复
- 修复:desc(type3) 自己拼接 + 跳过空组($func.getConditionStr),无组→"请设置条件",部分配置→只显示已配组(无重复)
- 进一步:严格判定已配 = columnId≠0 && zdy1 已填(选了字段没填值仍视为未配 → "请设置条件"提示去填)
- 进一步:去掉"条件组N:"前缀(标题已有"条件N"避免重影),单组直接显示"【input: hello】",多组用 且/或 连接

## 新增:网关分支折叠/展开(18:25)
- 需求:条件分支(含动态/条件并行)分支多时上下占用大,加折叠图标
- 实现(纯视图状态,数据零改动):
  - layout.js: layoutFlowTree 第 3 参 foldedIds(Set);measureGateway/placeGateway 折叠分支——折叠网关单元=FOLDED_W(210)×NODE_H,主链直连(exit=右缘中点→childNode),无泳道/汇合/加号;gateway entry 带 folded+branchCount
  - index.vue: collapsedGwIds ref(Set),relayout 传入;toggleFold(gw)
  - 渲染:展开态=菱形+折叠按钮(菱形下方 y+24 常显,双V向内图标),加分支按钮下移到 y+44;折叠态=矩形(meta.fill 底+meta.stroke 描边)+网关类型图标+标题+副标题"已折叠·N个分支"+展开按钮(右三角)
  - 折叠网关仍可选中开条件面板编辑

## 修复:分支链水平连线穿过链节点(18:37)
- 现象:条件分支多时 lane-out 水平线与该泳道的 childNode 链节点(在泳道节点同一 y 行)水平重叠/穿过
- 根因:lane-out 画在泳道节点中心 y=cy,但链节点 y 也是 cy(链节 y=cy-BR_H/2 与泳道同中心),同一 y 水平线穿过链节
- 修复:lane-out y 改为 cy+BR_H/2+4(泳道节点底边下方 4px, 完全离开链节 y 范围),无弧形最简修改
- 进一步:lane-in 改用斜线(M sx gcy L laneStartX (lcy+BR_H/2+4)), 跳过泳道 1-5 节点 y 范围, 视觉上不穿过泳道区域(数学+视觉双保险);汇合竖线底=maxLcy+BR_H/2+4 跟 lane-out 最低点对齐, 避免断点
1. autoFoldBigGateways(exceptId):分支数>FOLD_THRESHOLD(5)的网关,在移开焦点时自动折叠;selectNode/selectGateway/clearSelect 开头调用
   - exceptId 保护:选中节点所在祖先网关(ancestorGatewayId,nodeMap 向上找)不折叠——用户正在条件区编辑时其网关保持展开
2. toggleFoldAll:工具栏按钮(有折叠→全部展开,否则折叠全部有分支网关);hasFolded computed 控制按钮图标(双V向内=折叠全部/向外=展开全部)与文案

## 修复:切换节点后面板不切换(17:05)
- 现象:点发起人再点审批人,右侧面板仍是发起人的
- 根因:openConfig 只打开新面板(setXxx(true)),没关闭旧面板——closeAllDrawers 只在 selectGateway 里调了,普通节点切换漏掉;embed 模式下多个 drawer 同时 visible 垂直堆叠,旧的盖在上面
- 修复:openConfig 和 openCondition 开头加 closeAllDrawers()
- 面板映射核对(与 nodeWrap.setNodeInfo 一致):type1→promoter, 4/12/17/18/19/20/21→approver, 6→copyer, 7→approver(带分支index), 8/13→copyerV2, 9→autoNode, 2/3→condition

## 新增:退回/跳转关系线拖线交互(19:10)
- 需求:审批类节点(4/12/17/20/21,排除自动推进18/自动退回19)顶部/底部各3个交互点(共6个,上下对称):
  - 左点:不同意按钮→退回指定节点(重新开始) disagreeBackType=4
  - 中点:不同意按钮→退回指定节点(回到当前节点) disagreeBackType=5
  - 右点:同意按钮→跳至固定节点 isApproveForwardNode+forwardType=2+forwardNodeIds+labelList('approve_forward_node')
- 方向限制(严格落点):左/中点只能连"后"(流程方向后面的审批人,afterNodeIds 复用 availableForwardNodes 遍历逻辑);右点只能连"前"(发起人方向前面的发起人+审批人,beforeNodeIds 复用 availableBackNodes 的 nodeMap.parent 向上遍历)
- 交互:relDots(n) 算6点位置;startRelDrag 拖线(紫色虚线 hd-temp-rel)→命中节点高亮 hd-rel-target(紫色描边)→松手 applyRelation 写节点字段+syncRelationToPanel 同步面板 store 副本(按钮权限下拉实时更新);方向不对不可落
- 关系线渲染 relationEdges:退回红色虚线(从节点底部绕弧 relPath back),跳转蓝色虚线(从顶部绕弧),带标签(不同意退回·重新开始/回到当前/同意跳转);点击选中 hd-rel-sel,显示删除按钮 delRelation 清字段+同步面板
- 删除节点/分支时 cleanupRelationRefs 清理树中悬空引用(disagreeBackType/disagreeBackToNodeId/isApproveForwardNode/forwardNodeIds/labelList)
- 全部改动集中在 HorizontalDesigner/index.vue

## 修复:交互点显隐 + 方向限制修正(19:19)
- 问题1:交互点常显太乱 → 改为与右侧操作点一致:悬停节点/选中/正在拖线时显示;目标节点在拖线将要落到它身上时(relTargetKey 命中)显示脉动连接点(hd-rel-target-dot)
- 问题2:左点连不到左侧节点 → 根因是最初需求方向映射有矛盾("左侧的点只能向后连"但"向左连接=退回"),按业务直觉修正:退回(back4/back5 左/中点)连「前」= beforeNodeIds(前面的发起人+审批人);同意跳转(forward 右点)连「后」= afterNodeIds(后面的审批人)

## 修复:连线后右侧面板不即时刷新(19:28)
- 现象:画布拖线建立退回/跳转关系后,右侧按钮权限面板不刷新(实际已生效,切走再切回才看到);删除连线同样
- 根因:approverDrawer 的 watch(approverConfig1) 无 deep,只认 store.approverConfig1 整体引用;syncRelationToPanel 用 Object.assign 原地改 cfg.value 字段,引用不变 → 面板不刷新
- 修复:新增 CFG_SET(各 storeKey 对应 setter);syncRelationToPanel 改为整体替换——merged = {...cfg.value, 字段...}, 用 setter({value: merged, flag: cfg.flag, id: cfg.id}) 提交新引用,触发 drawer→ButtonStepPanel 的 watch 链重载配置;syncConfig(deep) 幂等无副作用

## 修复:关系线删除按钮被画布截断(19:32)
- 现象:单排流程时选中关系线,删除按钮(原来在标签正下方 y+22)被画布上下边界截断,难点击
- 根因:back 线标签在 max(底部)+24,删除按钮 +22 → 圆心 = 底部+46 > 画布高度(只留 PAD 余量),超出下边界
- 修复:删除按钮改为放标签右侧(同一水平线, x+labelW/2+16),垂直方向永不超边界

## 新增:工具栏隐藏/显示连线(19:38)
- 需求:退回/跳转虚线太多造成视觉混乱,工具栏"折叠全部"后加"隐藏连线"按钮
- 实现:relLinesHidden ref;关系线(relationEdges)v-if=!relLinesHidden;同时隐藏6个交互点(它们是产生这些线的入口);主流程固定连线(edges)不受影响;按钮带眼睛/斜线图标,文案"隐藏连线"/"显示连线"

## 修复:同意跳转关系线反显不显示(19:45)
- 现象:保存时写了 isApproveForwardNode=true,发布成功;但反显(detail 接口)时后端没返回 isApproveForwardNode 字段(只有 forwardType=2 + forwardNodeIds + labelList 里的 approve_forward_node),画布虚线不显示,右侧面板却能显示
- 根因:relationEdges 判断只看 node.isApproveForwardNode,反显数据缺该字段
- 修复:判断改为 `isApproveForwardNode || labelList 含 approve_forward_node`(与 ButtonStepPanel.loadApproveForwardConfig 一致),反显以后端返回的 label 标记为准
