# 2026-08-09 工作日志

## 流程节点角标图标来源(周老板问 # 图标)

**结论:截图里红色背景的 "#" 不是 SVG,是纯 CSS 字形。**

### 节点右上角的"node-badges"角标组(`nodeWrap.vue` 第 36-40 行 / 并行分支第 123-129 行)

```html
<span class="node-badges">
  <i v-if="noticeIconShow"    class="anticon anticon-notice notice"></i>
  <svg-icon v-if="timeoutNoticeIconShow" icon-class="time" class="timeout-notice" />
  <i v-if="labelIconShow"     class="anticon anticon-tag label-icon"></i>
</span>
```

三种图标的真实来源:

| UI | 模板 | 来源 | 文件 |
|---|---|---|---|
| 通知铃铛 | `<i class="anticon anticon-notice">` | **PNG 背景图** | `src/assets/images/antflow/notice-fill.png` |
| 超时时钟 | `<svg-icon icon-class="time">` | **SVG 文件**(svg-sprite) | `src/assets/icons/svg/time.svg` |
| **标签 #** | `<i class="anticon anticon-tag label-icon">` | **纯 CSS 伪元素** | **无文件** |

CSS 定义(`src/assets/styles/antflow/workflow.scss`):
- `.anticon-tag:before { content: "#"; font-weight: bold; }` — 字形
- `.node-wrap-box .label-icon { color:#fff; width:16px; height:16px; }` — 颜色/尺寸
- 红底来自节点 title 栏的 `background: rgb(${titleBgColor})`,不是 icon 自带背景

### 全项目 SVG 图标集中位置

- `src/assets/icons/svg/` — 70+ 个全局通用 svg(time / user / tree / build 等)
- `src/assets/icons/nodeSvg/` — 31 个节点专用 svg(approve / copy-user / finish-process / auto-drive-ahead 等)

注册机制:
- `main.js`: `import 'virtual:svg-icons-register'`
- 插件: `vite/plugins/svg-icon.js`(vite-plugin-svg-icons 风格)
- 组件: `src/components/SvgIcon/index.vue` — `<use xlink:href="#icon-${iconClass}">` 引用 sprite 里 `<symbol id="icon-xxx">`
- **symbolId 规则确认**:`icon-[dir]-[name]` 的 [dir] 只在文件位于 iconDirs 子目录时生效;本项目两个 iconDir 直接指向 svg/、nodeSvg/,故 symbolId 就是 `icon-{文件名}`,与 SvgIcon `#icon-name` 匹配,无需带目录前缀。

## 节点角标新增:表单权限 / 加批图标(周老板需求)

在 `nodeWrap.vue` 的 `.node-badges` 内(普通节点 36-43 行 + 并行分支 123-135 行)新增 3 个 svg 角标:

```html
<svg-icon v-if="formPermEditableShow" icon-class="edit" class="node-badge-icon" />      <!-- 表单权限含可编辑 -->
<svg-icon v-if="formPermHiddenShow"  icon-class="eye" class="node-badge-icon" />        <!-- 表单权限含隐藏 -->
<svg-icon v-if="signUpShow"          icon-class="add-sign" class="node-badge-icon" />  <!-- 按钮含加批 -->
```

**判定字段(关键)**:
- 表单权限:节点存 `lfFieldControlVOs` 数组,每项 `{fieldId, fieldName, perm}`,`perm` ∈ `R`(只读)/ `E`(可编辑)/ `H`(隐藏)。drawer 表单权限设置=FormStepPanel → FormPermConf(`drawer/panel/FormStepPanel.vue`、`drawer/permConfig/FormPermConf.vue`)
- 加批:按钮权限 `buttons.approvalPage` 数组,`buttonType == 19` 为加批(同步字段 `isSignUp`)。普通审批节点默认 approvalPage=`btns(3,4)` 不含加批;**并行审批节点(createParallelNode)默认 `btns(3,4,18,19,21)` 含加批** → 新建并行审批人默认显示 add-sign 角标(产品默认语义)
- 按钮枚举:同意 3 / 不同意 4 / 转办 18 / 加批 19 / 推进 42 等,定义在 `utils/antflow/const.js`(approvalButtonConf)

**样式**:`workflow.scss` 新增 `.node-wrap-box .node-badge-icon`(16x16、color:#fff、flex-shrink:0),与 timeout-notice 一致。

**注意**:add-sign.svg 为多色图标(内部硬编码 #2563EB/#64748B 等),不随白色样式变;edit/eye 为单色描边会变白。HorizontalDesigner(横向)无角标体系未动;发起人节点(nodeType=1)无角标未加。

## Bug 修复:加批角标不显示(数字数组格式兼容)

**根因**:后端返回的 `buttons.approvalPage` 是**纯数字数组**(mock: `"approvalPage": [3, 4]`),而 nodeWrap 判定用 `some(b => b.buttonType == 19)`(对象格式),数字没有 `.buttonType` → 永远 false → 角标不显示。表单权限 `lfFieldControlVOs` 是对象数组所以正常。流程加载时无统一格式化按钮(已确认),drawer 面板(ButtonStepPanel/ButtonPermDialog)都做了数字兼容,唯独 nodeWrap 渲染层没做。

**修复**:nodeWrap.vue 新增 helper `hasButtonType(btnList, type)`(兼容数字数组 `[3,4,19]` 与对象数组 `[{buttonType:19}]`),并替换 4 处判定:
- `signUpShow`(加批角标 19)
- `hasSignUpButton`(并行分支加批)
- 模板 42 推进图标(`approver-drive-ahead`)
- `titleBgColor` 里 42 推进着色

**经验**:本项目按钮配置类字段(buttons.approvalPage/viewPage/startPage)后端存数字数组、前端面板操作后转对象数组,所有基于 buttonType 的判定必须用 `hasButtonType` 式兼容,勿直接用 `b.buttonType == x`。

## 转办角标 + hover 关闭按钮修复

1. **转办角标**:按钮权限选中转办(buttonType=21)显示 `transfer-assignee.svg`(svg/ 目录,单色 currentColor)。nodeWrap.vue 普通节点 `transferShow` + 并行分支 `hasTransferButton`,复用 `hasButtonType(approvalPage, 21)`。样式复用 `.node-badge-icon`。
2. **hover 关闭按钮 bug**(重要!):`.node-badges` 容器与 `.close` 都定位在 `right:10px; top:50%` 重叠,DOM 顺序 close 在前、node-badges 在后(层级更高),hover 时新增角标(edit/eye/add-sign/timeout)未随 notice/label 隐藏 → 盖住关闭按钮且拦截点击。修复:workflow.scss 把 hover/active 隐藏规则从"仅 .notice/.label-icon"升级为**隐藏整个 .node-badges 容器**(visibility:hidden 保留占位防位移)。**以后往 node-badges 加角标不用再逐个加隐藏规则。**
3. 节点角标现状:notice(PNG)/timeout(svg time)/label(#CSS)/edit/eye/add-sign/transfer-assignee,判定见上。

## 退回语义角标(process-back.svg)

- 判定:`hasButtonType(buttons?.approvalPage, 18)`(选中退回按钮) **或** `!!nodeConfig.disagreeBackType`(不同意按钮行为非默认)
- **关键字段**:不同意按钮行为存 `node.disagreeBackType` —— 默认(结束流程)存 `null`(ButtonStepPanel 里 `disagreeBackType=0` → 存 null),退回发起人=2、退回指定节点=4/5 → 存 2/4/5。判定用 truthy 即可覆盖 2/4/5,排除 null/0/undefined。
- 普通节点 `backShow` + 并行分支 `hasBackSemantics`,图标 `svg/process-back.svg`(单色 currentColor)。
- 注意与 `drawBackType`(退回按钮行为)区分:drawBackType 是"退回"按钮的子配置,disagreeBackType 是"不同意"按钮的行为,两者独立。

## 推进语义角标(process-jump-ahead.svg)

- 判定:`hasButtonType(approvalPage, 42)`(推进按钮) **或** `labelList` 含 `approve_forward_node` **或** `isApproveForwardNode === true`(同意按钮行为非默认=同意推进)
- **关键**:同意推进的稳定判据是 label `approve_forward_node`(后端持久化),`isApproveForwardNode` 是前端临时标记,两个都查。**推进按钮(42)与同意推进(label)互斥**(ButtonStepPanel 勾选同意推进会 splice 移除 42)。
- 普通节点 `jumpAheadShow` + 并行分支 `hasJumpAhead`,图标 `svg/process-jump-ahead.svg`。
- 与 titleBgColor 中 FORWARD_APPROVE_COLOR 的 label/42 判据同源。

## 协助角标(assist.svg)

- 判定:`hasButtonType(approvalPage, 41)`(协助按钮)。
- **注意**:`assist.svg` 在 `svg/` 和 `nodeSvg/` **两个目录都有**——svg-sprite 的 symbolId 都是 `icon-assist`(目录为根时 [dir] 为空),后注册的覆盖先注册的,`icon-class="assist"` 引用同一个 symbol,无实际冲突,但内容以生效的那个为准。协助节点(nodeType=17)在 ButtonStepPanel 里被强制勾选 41,故保存后其角标必显示。
- 普通节点 `assistShow` + 并行分支 `hasAssistButton`。

## 角标折叠(>6 截断 + more-up)

- 需求:角标总数 > 6 时只显示前 5 个 + `more-up.svg`(竖向三点),≤6 全显示。
- **实现**:nodeWrap.vue 角标渲染从"分散 v-if"重构为**配置数组 v-for**:
  - 普通节点:`badgeList` computed(10 项,含 notice/timeout/label/edit/eye/add-sign/transfer/back/jump/assist)→ `visibleBadges`(>6 截前 5)+ `showMoreBadges`
  - 并行分支:`getBadgeList(item)` 函数返回 `{list, more}`,模板调用两次
  - 每项结构 `{key, show, type: 'anticon'|'svg', cls/icon}`;anticon 渲染 `<i :class>`,svg 渲染 `<svg-icon :icon-class>`
- 截断阈值:>6 → slice(0,5) + more。逻辑已单测(0/5/6 全显,7/8/10 → 5+more)。
- **以后加角标只需在 badgeList/getBadgeList 数组里加一项,不用再改模板。**
- more-up.svg 在 svg/ 目录,单色。

## 审批方式角标(signType 映射)

- 判定:`node.signType` 映射图标 —— 1会签→`all-sign.svg` / 2或签→`or-sign.svg` / 3顺序会签→`all-sign-in-order.svg` / 4仲裁签→`arb-sign.svg`(4 个都在 svg/,单色)。
- **重要**:signType 默认就是 1(createApproveNode/createParallelNode 均 `signType: 1`),故**所有审批类节点默认都会显示 all-sign 角标**;发起人/抄送无 signType 不显示。
- 普通节点 `signModeIcon` computed + 并行分支 `getSignModeIcon(n)`;badgeList 中 sign-mode 放第 1 位(折叠时优先保留)。
- 用 `==` 宽松比较(与项目其他 signType 判断一致,兼容后端字符串)。

## 额外审批角标(extra-*-sign)

- 字段:`node.property.additionalSignInfoList`,每项 `{propertyType(1=增加,2=排除), nodeProperty, signInfos}`;**"有值"判据**:存在 propertyType 匹配且 `signInfos` 非空的条目(面板 setExtraSignInfos 会在 signInfos 清空时删除条目,但历史脏数据可能残留空项,故判据加 signInfos 非空)。
- 组合:双有 → `extra-add-and-minus.svg`;仅加 → `extra-add-sign.svg`;仅排 → `extra-minus-sign.svg`;都无 → 不显示。
- 普通节点 `extraSignIcon` computed + 并行分支 `getExtraSignIcon(n)`;badgeList 中 extra-sign 排第 2 位(审批类图标优先)。3 个 svg 均在 svg/,单色。

## 审批人类型数字徽章(变体B, CSS 实现)

- **需求**:NodePropertyEnum 15 种审批人类型(2层层/3层级/4角色/5人员/6HRBP/7自选/8业务表/11外部/12发起人/13直属领导/14部门负责人/15被审批人/16表单相关/17自定义/18上一节点),每个做图标太多 → 用 CSS 数字徽章,`node.setType` 是几就显示几。
- **变体 B(周老板选定)**:圆角方(4px)半透明白底 + 白色描边 + 白字 11px(双位数 .wide 降 10px),16×16,flex 居中。CSS 在 workflow.scss `.node-badge-num`。
- **实现**:badgeList/getBadgeList 新增 `type:'num'` 项 `{key:'set-type', num: node.setType, tip: setTypeTip}`;模板渲染分支 `<span v-else-if="b.type==='num'">`;tip 来自 const.js `setTypes` 的 label(悬停显示类型名)。
- **注意**:setType 默认 5(createApproveNode),故所有审批节点默认显示"5"徽章(指定人员),与 signType 默认全显同理;发起人/抄送无 setType 不显示。
- 对照表见 NodePropertyEnum.java / const.js setTypes。

## 角标逻辑抽公共模块 + 横向设计器补角标

- **新增 `src/utils/antflow/nodeBadges.js`**:角标判定统一单一来源 —— `hasButtonType`/`hasTimeoutRemind`/`hasNotice`/`hasLabel`/`hasFormPermEditable/Hidden`/`hasSignUpButton`/`hasTransferButton`/`hasAssistButton`/`hasBackSemantics`/`hasJumpAhead`/`getSignModeIcon`/`getExtraSignIcon`/`getSetTypeNum/Tip` + `getBadgeList(node)` 返回 `{list, more}`(>6 截前5)。badge 项结构 `{key, type:'svg'|'num'|'anticon', icon?/num?/tip?/kind?/cls}`,anticon 用 `kind` 区分 notice(铃铛 PNG)/label(#)。
- **nodeWrap.vue 重构**:badgeList computed = `getNodeBadges(props.nodeConfig)`,并行分支用 `getNodeBadges(item)`;删除全部旧判定 computed/函数(共 ~30 个)。**注意**:模板 42 推进图标和 titleBgColor 里仍用 `hasButtonType`,需从公共模块一并 import,否则 ReferenceError。
- **HorizontalDesigner(index.vue)补角标**:SVG 渲染 —— 节点 g 内加 `<g class="hd-badges">`,`transform=translate(n.x+n.w-10, n.y+15)`,角标往左排 `-((len-bi)*20+10)`;svg 图标用 `<use #icon-x>`、数字徽章用 `<rect rx=4 + <text>`(变体B)、label 用 `<text>#</text>`、notice 用 `<image :href=noticeImg>`(import notice-fill.png);`<title>` 做悬停提示。**hover 时隐藏**(`v-if="hoverKey !== n.key"`)让位删除按钮;条件节点(nodeType 3)不显示。
- 竖向与横向共用 nodeBadges.js,**以后加角标只改公共模块一处**。

## 横向角标右对齐修复

- **bug**:角标 transform 原用 `-((len-bi)*20+10)` 固定预留给 more 20px 槽位,无 more 时整组偏左 30px。
- **修复**:新增 `hdBadgeOffset(len, bi, more)` —— 最右元素(最后一个角标或 more)中心距节点右边缘 10px,每项 20px 向左排;有 more 时角标再左移 20px 让位。已单测(4角标无more → -70,-50,-30,-10;有more → 角标 -90..-30 + more -10)。

## 横向角标统一白色(最终, 周老板拍板)

- 黑色"显示有点脏" → 改回**统一白色**:
  - svg `<use>`:`fill="#fff"` + **`color="#fff"`(关键!currentColor 类图标取 color, 只设 fill 仍黑)**
  - 数字徽章:半透明白底 + 白描边 + 白字(变体B)
  - label `#` 白
  - notice 红铃铛 PNG:`filter: grayscale(1) brightness(3)` 红转白
- add-sign.svg 硬编码多色不受影响(如需统一白得改 svg 文件)。
- **经验**:SVG `<use>` 引用 sprite 时,currentColor 类图标(stroke/fill="currentColor")颜色取 use 的 `color` 属性,无 fill 的继承类取 `fill`;要全部控制必须 fill 和 color 同时设置。

## 横向角标图例 + 节点过滤高亮(周老板需求)

- **功能**:工具栏「隐藏连线」后加角标图例(13 种类型,与 nodeBadges 的 key 对应):hover 显示含义(title),点击切换选中(多选),命中的节点(同时含全部选中属性, **AND 过滤**)蓝色描边 + 闪烁 3 次(CSS animation `hd-badge-flash`),图例项选中态蓝色边框,右侧「重置」恢复。
- **成本评估结论(已给周老板)**:遍历 = 每节点 getBadgeList(13 项 filter)+ Set 比较,O(节点数×13),百节点毫秒级,可接受;无网络/存储成本。**做**。
- 实现:HorizontalDesigner/index.vue —— `BADGE_LEGENDS` 静态配置(render: svg/num/label/notice)+ `selectedBadgeKeys` ref + `toggleBadgeFilter`/`resetBadgeFilter`/`nodeBadgeKeys`/`isBadgeMatched`;节点 rect 加 `hd-badge-match` class。图例图标在工具栏白底用深色(#576a95),节点上角标仍白色。
- 注意:图例里 `<use>` 必须包在 `<svg>` 容器内(HTML 中不能裸放 use);数字徽章图例用示例 "5"。

## 图例交互升级(单选+Ctrl加选, 过滤前展开全部)

- **单选/加选**:`toggleBadgeFilter(key, ev)` —— 普通点击=单选切换(点已选中的唯一项则清空);`ev.ctrlKey || ev.metaKey`=加选/减选。模板 `@click="toggleBadgeFilter(lg.key, $event)"`。AND 过滤逻辑不变。
- **过滤前展开全部**:`expandAllForFilter()` —— `collapsedGwIds.value.size > 0` 时清空 Set + `relayout()`(与工具栏"展开全部"同逻辑,直接改状态,非模拟点击),保证折叠分支里的节点也参与过滤高亮。已单测 7 组选择用例全过。

## 过滤命中节点持续闪烁(直到点击图例以外任意区域)

- CSS `hd-badge-flash` 改 `infinite`(持续闪,不再 3 次停)。
- 停止机制:hd-root 加 `@click="onRootClick"` —— 点击不在 `hd-legend` 图例区内(resolve `legendRef.contains`)的任意位置(画布空白/节点/工具栏其他按钮)都 `resetBadgeFilter()` 清空选中 → class 移除 → 闪烁停止。图例区内的点击由图例自身 toggle 逻辑处理,不受影响。

## 数字徽章图例改为类型下拉(点击选具体审批人类型)

- **问题**:固定"5"只能代表指定人员,尴尬。
- **方案**:BADGE_LEGENDS 移除 set-type 项,在工具栏**独立渲染 el-dropdown**(trigger=click):下拉项 = const.js `setTypes` 全部(数字+名称);选中后 `selectedSetTypeNum` 设值,图例显示 `setTypeLegendNum = selectedSetTypeNum ?? 5`;active 态 = selectedSetTypeNum != null。
- **过滤**:`isBadgeMatched` 增加维度 —— `selectedSetTypeNum != null` 时要求 `node.setType == 所选值`(与图例 key 的 AND 叠加)。单选语义:选类型清空其他图例;普通点击其他图例清空类型。重置/onRootClick 同时清 `selectedSetTypeNum`。
- el-dropdown 的 command 回调 `@command="onSetTypeLegendSelect"`;下拉项样式 inline-block 数字列对齐。

## 推进语义角标(process-jump-ahead.svg)

- 判定:`hasButtonType(approvalPage, 42)`(推进按钮) **或** `labelList` 含 `approve_forward_node` **或** `isApproveForwardNode === true`(同意按钮行为非默认=同意推进)
- **关键**:同意推进的稳定判据是 label `approve_forward_node`(后端持久化),`isApproveForwardNode` 是前端临时标记,两个都查。**推进按钮(42)与同意推进(label)互斥**(ButtonStepPanel 勾选同意推进会 splice 移除 42)。
- 普通节点 `jumpAheadShow` + 并行分支 `hasJumpAhead`,图标 `svg/process-jump-ahead.svg`。
- 与 titleBgColor 中 FORWARD_APPROVE_COLOR 的 label/42 判据同源。

## 到达前设置(动态审批人)需求 grill-me 设计(周老板需求)

用 grill-me 技能逐点确认 8 个决策,产出设计文档 `doc/到达前动态审批人设计.md`。

**需求**:审批人节点 drawer 审批人设置 tab 新增「到达前设置」,节点用虚拟审批人占位,发起时虚拟人作 assignee,运行到该节点调 `FormOperationAdaptor.provideCurrentNodeAssignees(businessDataVo)→List<BaseIdTranStruVo>` 查真人,委托(setAssignee)给真人。

**8 决策**:
1. businessDataVo==null(仅 timer/async/重试/迁移链路)+assignee==-5 → 抛异常(回滚非用户事务,不影响用户审批)。
2. 新增 `AFSpecialAssigneeEnum.ARRIVAL_DYNAMIC_ASSIGNEE(-5)`(不复用 -3/-4)。
3. 新建 `NextNodeDynamicAssigneeProcessor implements AntFlowNextNodeBeforeWriteProcessor`,order=0(在 entrust=1 前,委托可叠加),门控 assignee==-5。参照 `FormRelatedAssigneeRefreshProcessor`(setAssignee+bpm_flowrun_entrust 审计 action_type=5)。
4. 多人:首个 setAssignee;其余循环调 `AddAssigneeProcessImpl` 加签(参考 `DefaultTaskFlowControlService` line243-258)。
5. 时序:setAssignee+空值跳过(complete)同步在 listener(有 NextNodeForwardProcessor/NextNodeLabelsProcessor `((TaskEntity)delegateTask).complete(varMap,false)` 先例);加签延迟事务 afterCommit(任务落库后 split/MultiCharSign 才安全,afterCommit 早于用户看到任务无竞态)。
6. `LowFlowApprovalServiceAspect` 新增独立 @Around for provideCurrentNodeAssignees(不复用 logAndProceed 前置+void):LowFlowApprovalService 默认返 emptyList;aspect proceed(noop)→按 beanName==formCode 匹配 LFFormOperationAdaptor→返回其结果;无匹配返 emptyList(→跳过)。
7. `FormOperationAdaptor` 加 `default provideCurrentNodeAssignees(T vo)` 返 null(同 loadNodeConfigJson);submit 侧零改动,-5 设计期作 setType=5 emplIds=[-5] 透传(同 -3)。
8. 前端复用 setType=5+emplIds=["-5"],signType 强制单人。

**关键代码事实**:businessDataVo 来自 ThreadLocal `AF_RUNTIME_BUISINESS_INFO`,只在 `BpmnSendMessageAspect.around`(doProcessButton 切面) set,submit/非submit 分支都在 proceed 前 set、后 remove;TaskListener 在 proceed 内同步触发→happy path 必有 vo。`NextNodeLabelsProcessor` 已在 create-listener 同步 `((TaskEntity)delegateTask).complete(varMap,false)`(line670/729)→跳过有先例。`AddAssigneeProcessImpl` 非会签走 `taskFlowControlService.split`、会签走 `MultiCharSign`,依赖 `createTaskQuery` 查已落库任务→listener 内调是反模式,故加签必须 afterCommit。

## 到达前设置(动态审批人)实施完成

按设计文档落地后端 5 处 + 前端 2 处。设计稿移至 `.scratch/到达前动态审批人设计.md`。

**后端(antflow-base/antflow-engine,周老板已验证能启动)**:
1. `AFSpecialAssigneeEnum` 加 `ARRIVAL_DYNAMIC_ASSIGNEE(-5,"-5","到达前动态查询审批人")`。
2. `FormOperationAdaptor` 加 `default List<BaseIdTranStruVo> provideCurrentNodeAssignees(T vo)` 返 null(同 loadNodeConfigJson),import List+BaseIdTranStruVo。
3. `LowFlowApprovalService` override `provideCurrentNodeAssignees(UDLFApplyVo)` 返 `Collections.emptyList()`。
4. `LowFlowApprovalServiceAspect` 加独立 `@Around`(不复用 logAndProceed):proceed(noop)→按 beanName==formCode 匹配 LFFormOperationAdaptor→返其结果;无匹配返 emptyList。import Collections+BaseIdTranStruVo。
5. 新建 `NextNodeDynamicAssigneeProcessor implements AntFlowNextNodeBeforeWriteProcessor`,order=0。门控 assignee==-5;vo==null 抛异常;formAdapter.provideCurrentNodeAssignesses;空→复用 AUTO_NODE_SKIP `((TaskEntity)delegateTask).complete(varMap,false)` 跳过+verifyInfo;非空→首个 setAssignee+bpm_flowrun_entrust 审计,余人注册 `TransactionSynchronizationManager.registerSynchronization` afterCommit + `new TransactionTemplate(txMgr).execute` 新事务循环调 `AddAssigneeProcessImpl`(参考 ForwardToNodeImpl#advanceToTargetNode + DefaultTaskFlowControlService line243-258)。

**周老板修正**:`BpmFlowrunEntrust.actionType` 只有 0(委托)/1(change assignee)/2(add)/3(remove),我原写 5 越界。改为 **0(委托)**(虚拟人委托给动态查到的真人,与用户委托同语义)。

**前端(antflow-vue,照搬 PREV_NODE_APPOINTED radio 19 范式)**:
6. `utils/antflow/const.js`:`setTypes` 加 `{value:20,label:"到达前设置"}`;新增 `ARRIVAL_DYNAMIC_SET_TYPE=20`/`ARRIVAL_DYNAMIC_VIRTUAL_USER_ID="-5"`/`ARRIVAL_DYNAMIC_VIRTUAL_USER_NAME="到达前动态查询审批人"`。
7. `drawer/panel/ApproverStepPanel.vue`:`displaySetType` get/set 处理 20(与 19 互斥,选中写 setType=5+nodeApproveList=[{targetId:"-5"}]+signType=1+isArrivalDynamic=true);模板人员选择器 v-show 排除 20+加 tip;changeType 对 20 跳过清空。`isArrivalDynamic` 仅前端反显,后端无需识别(触发靠 assignee==-5 非标签)。

**环境坑**:本机 mvn(bash 脚本)因 Git Bash /c/ 路径 Windows java 不认而失败;改用 `C:/greensoft/apache-maven-3.6.3/bin/mvn.cmd` 直接调用可行(Java 8)。前端 pnpm 9.15.0 需 `env -u NODE_OPTIONS pnpm`(NODE_OPTIONS 含 --use-system-ca 被 node 拒)。`build:prod`=`vite build`。

**关键范式对照**:`PREV_NODE_APPOINTED`(-4) 是"虚拟审批人运行时解析"的完整先例——前端用新 radio 值(19)+displaySetType 映射+setType=5+虚拟用户+标志位;后端用节点标签 `af_syslabel_prev_node_appointed` 在 NextNodeLabelsProcessor 触发。我的"到达前设置"(-5)前端照搬,后端区别是**触发靠 assignee==-5 不靠标签**,故无需 label。

## 到达前设置 前端两处修正(周老板反馈)

1. **反显不选中"到达前设置"而是"指定人员"**:`isArrivalDynamic` 标志位在 save→load 往返中丢失导致反显失效。改用**持久化数据检测**:`displaySetType` getter 判断 `setType==5 && nodeApproveList 含 targetId=="-5"` → 返 20。-5 作为 emplId 必然持久化(否则 submit 也不工作),比标志位可靠。**去掉 isArrivalDynamic 标志位**。切换到其它类型时 changeType 会清空 nodeApproveList,无 -5 残留误判。
2. **tip 文案精简**:去掉"多人加签/查不到跳过"细节,只保留"审批人在流程运行到该节点时由业务代码(FormOperationAdaptor.provideCurrentNodeAssignees)动态查询指定"。

**经验**:虚拟审批人节点的反显优先用"虚拟人 id 在 nodeApproveList 里"检测,不要依赖额外标志位(标志位易在前后端往返中丢失);真实审批人 id 不会与虚拟人 id(-3/-4/-5)冲突,检测可靠。
