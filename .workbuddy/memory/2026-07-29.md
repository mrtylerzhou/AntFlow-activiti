# 2026-07-29 — AntFlow 专题文章 + 截图

## 完成内容

### 新建 6 篇专题文章（docs-site/topics/）
1. **multi-person-approval.md** — 多人审批与会签模式详解：12 种审批人来源 × 4 种会签模式（会签/或签/顺序会签/仲裁签）× 3 种空审批人策略，含运行时行为对比表、JSON 存储结构
2. **reject-and-return.md** — 驳回与退回机制全解：3 种退回目标（上一节点/发起人/任意节点）+ 6 种快捷评语 + 不同意 vs 退回区别，含撤销/退回列表截图
3. **addsign-transfer-delegate.md** — 加签、转办与委托实战：审批人加批 vs 运维加签 vs 转办 vs 委托的完整对比，含委托对话框和加签运维页截图
4. **condition-routing.md** — 条件分支与动态路由：5 种条件节点类型 + 条件组 AND/OR 逻辑 + Drools/Java SPI 双模式条件引擎 + JSON 配置结构
5. **ops-monitor.md** — 流程运维与监控实战：9 种运维操作（减签/加签/增删节点/推进/变更/撤回/作废/转发）+ 全局高级设置开关 + 消息模板管理
6. **design-complete-flow.md** — 从零设计一个完整流程：手把手 9 步教程（创建分类→设计器4步向导→配置会签→条件分支→发布→测试），嵌入 12+ 张截图

### 截图素材（35 张，docs-site/public/images/topic-*.png）
全部使用 agent-browser 在亮色主题下截取，覆盖：
- 登录选人、首页仪表盘、待办列表、审批详情（3 个 tab）
- 退回弹窗、加批弹窗、转办弹窗、人员选择器
- 流程设计器（画布/添加节点菜单/审批人面板/5 个 tab/仲裁签）
- 条件分支画布、条件配置面板
- 向导高级设置、流程监控列表、运维更多菜单(9项)、加签运维页
- 委托设置/对话框、消息模板、抄送列表、撤销退回列表
- 流程中心、发起表单、我的发起详情

### VitePress 配置更新
- .vitepress/config.ts：导航栏新增「专题文章」下拉菜单；侧边栏新增 `/topics/` 分组（6 篇文章）

### 技术要点
- agent-browser 的 el-dropdown hover 触发需用 JS dispatchEvent('mouseenter') 才能打开
- 默认暗黑主题切换：点击 `.theme-switch-wrapper` 即可
- VitePress dev 模式是 SPA，curl 验证只能看 HTTP 200，内容靠浏览器渲染
