# 2026-08-17

## 版本比较节点差异精细化(用户反馈, 2026-08-17)

用户反馈三点: ①审批人类型 13→12 无汉字含义、顶层 nodeProperty 与 property 双报、"冗余"字样; ②表单字段权限只显示 JSON 不可读, 要表格化; ③高级设置 deduplicationExclude 只显字段名无业务含义。

已实现(纯前端, versionCompare.js + index.vue):
- **枚举汉字**: `ENUM_DICTS.nodeProperty` 改用 const.js `setTypes` 全量映射(5=指定人员/13=直属领导/12=发起人自己/7=发起人自选等), `setType` 与 nodeProperty 共用同一字典(旧字段名同语义, 顶层镜像走 setType leafKey 也命中); 补 `batchStatus {0:禁止,1:允许}`; fmtValue 输出「汉字(数字)」格式, 未命中保持原值。signType/forwardType/drawBackType 等映射原本就有。
- **镜像去重**: 新增 `MIRROR_LABELS`(nodeProperty/setType/signType/directorLevel/nodeApproveList/formAssigneeProperty/formInfos 顶层 → 与 property.* 相同汉字 label), rows.add 的 label+源值+目标值去重自动消双报; FIELD_LABELS.setType 去掉「(冗余)」二字。关键坑: 顶层镜像 setType 的 leafKey 是 setType, 字典必须有 setType 键否则裸数字导致去重失效(测试抓出)。
- **表单权限表格化**: `lfFieldControlVOs`/`formHidden` 从通用 diff 摘出, 新写 `diffFormPerms(s,t,formNameCtx)` → `{groups:[{name, hidden|null, fields:[{fieldId,fieldName,sourcePerm,targetPerm,status}]}], hiddenDiff}`; 分组: 内联=主表单(与 extractForms 命名一致), 外部多表单按 formdataId 分组(组名来自 buildFormNameMap: conf.lfFormdataList → formdataId→formdataName, compareConfs 里合并两版传入); 字段对齐键 fieldId, **全量对照含未变**(用户明确要全量); perm R/E/H → 只读/可编辑/隐藏(渲染层); formHidden 整表隐藏变化单独行。formPermTable 行带 formPermCount 计入 nodeDiffCount。
- **高级设置语义**: FIELD_LABELS 补 `deduplicationExclude:'抗去重(该节点不参与审批人去重)'`、`batchStatus:'批量审批'`(值枚举走 batchStatus 字典 允许(1)/禁止(0))。节点高级设置 tab 实际字段就 batchStatus + deduplicationExclude(+labelList 已在基本信息比过); 节点 extraFlags 是流程级位标志镜像, 基本不变。
- 前端 index.vue: 节点差异区 section 计数改 `sec.count`(formPermTable 行按 formPermCount 计); 渲染 formPermTable 行 → 权限对照表(字段|源版本|目标版本), 未变灰显/变化高亮/新增删除标签, 多表单分组 + 整表隐藏行; 辅助函数 plainRows/formPermGroups/permName。
- diffNodePair 签名: `(pair, sList, tList, formNameCtx)`。

测试: `.scratch/tmp/test_versionCompare.mjs` 新增断言(审批人类型一行+汉字、多人审批方式一行+汉字、批量审批 允许(1)→禁止(0)、抗去重 关→开、表单权限全量对照 f1 changed/f2 same/f4 removed、formPermCount=2), esbuild bundle + node 全过。

## 按钮权限表格化 + 表格结构统一(用户反馈续, 2026-08-17)

用户反馈: 按钮权限仍只显示差异 JSON, 无按钮开关对比。

- **按钮数据结构**: `node.buttons = { approvalPage: [{buttonType, buttonName}], viewPage: [...] }`; buttonType 枚举在 const.js `approvalButtonConf`(**注意值**: agree=3/noAgree=4/repulse=18 退回/addApproval=19 加批/transfer=21 转办/oppose=39/assist=41/forwardToNode=42/drawBackAgree=32 撤销同意; 6=退回上节点修改, 7=作废 不是转办/加批); 按钮全集 = `approvalPageButtons`(审批页 8 个) + `nodeViewPageButtons`(查看页)。
- **统一表格结构**: formPerm 与 buttons 共用 `{tableType:'formPerm'|'buttons', groups:[{name, hidden|null, fields:[{key,label,source,target,status,tag}]}], diffCount}`; 引擎汉字化 perm(R/E/H→只读/可编辑/隐藏), 前端一套模板渲染(kind='table' 行), 表头按 tableType 显示「字段」或「按钮」; plainRows 排除 kind==='table'。
- `diffButtons(s,t)`: 按按钮枚举全集逐按钮对照「显示/显示·自定义名/隐藏」, 对齐键 buttonType, 未变灰显/变化高亮; 审批页面+查看页面两组; 返回 null 当两版 buttons 均为空。
- **关键坑**: 挂载 table 行时误用 `sections.get(sec).pop()` —— pop() 会把行从数组移除, sections 变空, Object.assign 改的是已移除对象 → section 计数/渲染全丢。必须用 `list[list.length-1]` 取末行不改数组。调试时 bundle 里中文被 esbuild 转成 \uXXXX 转义, grep 中文字符串找不到, 用 node indexOf 检查。
- 测试: mock buttons(源 同意+转办(21)自定义名'转给同事' / 目标 同意+加批(19)), 断言 byBtn 3/4 same、21/19 changed、显示·转给同事→隐藏、diffCount=2; 全过。

## 流程诊断功能实施完成(2026-08-17 晚)

设计文档: `.scratch/process-diagnosis-design.md`。全部落地:

**后端**(mvn 编译通过):
- `BpmVerifyInfoVo` +`nodeId`; `BpmVerifyInfoMapper.xml` getVerifyInfo left join AF_HI_TASKINST(h.ID_=w.task_id) 取 h.NODE_ID_, findTaskInfor 加 t.NODE_ID_; 列名都带下划线 `NODE_ID_`。null 检测在 BpmVerifyInfoBizServiceImpl 尾部(id 非空且 nodeId 空 → log.error, 发起/流程结束/预测虚拟节点无 id 自动豁免)。
- 新接口: `GET /bpmnConf/diagnosisInit?processNumber=` + `POST /bpmnConf/diagnoseNode`; 实现 `ProcessDiagnosisBizServiceImpl`(service.biz) + 接口(service.interf.biz)。
- 三个新 VO(base): ProcessDiagnosisInitVo / NodeDiagnosisVo(含 BranchEvaluation/ConditionItemResult/EntrustRecordVo/SignupRecordVo/RawTaskVo 内部类) / NodeDiagnosisRequestVo。
- diagnoseNode 核心复用: `AutoConditionEvaluator`(引擎同款条件求值, optType 1>=2>3<=4<5==6-9介于); conf 查 `bpmnConfBizService.detail(bpmnCode)`(String 重载, nodes 已含 property.conditionList); 表单值 `formFactory.dataFormConversion(params,formCode)`+queryData, LF 反射读 lfFieldsMulti, DIY declared fields 到 BusinessDataVo 为止。
- 归因: present(hi∪ru node_id) → NOT_REACHED(先序序比较, buildPreOrderIndex 从 type1 起按 分支sort在前/主干在后 DFS) → CONDITION_MISS(同 nodeFrom 的 type3 兄弟分支横评) → SIGN_SKIP(entrust actionType=3) → UNKNOWN。
- **坑**: VariableConfigJson.SignUpItem 无 getter(仅 personnelByElement 手写), 用 fastjson2 JSONObject 解析; QueryWrapper orderByDesc 用字符串列名别用 lambda(重载歧义); mvn 需 `-Dgpg.skip=true`(且 PowerShell 下要加引号 `"-Dgpg.skip=true"`, 否则被拆参)。

**前端**:
- 公共组件抽取: `components/Workflow/FlowTree/{flowTree,flowNode}.vue`(compareTree/cmpNode 迁移, `__mark ?? __diff` 泛化, 新增 st-hit/st-current/st-miss 样式, ft- 前缀); versionCompare/index.vue 改引用, 旧文件已删。
- `components/Workflow/FlowDebug/debugPanel.vue`(flowDebug 主体抽取; props confId/initiator只读/prefillValues; LF 预填走 lfFieldsData, DIY 预填 diyFormData 多选拆数组); flowDebug/index.vue 变薄壳。
- `views/workflow/components/diagnosisDialog.vue`: 全屏 94% dialog 三栏(FlowTree | 审批路径 timeline | el-tabs 节点诊断/调试); 图 __mark hit/current, __pairKey=node.id; 路径点击→activeKey 联动; nodeId null 标灰+console.error; 节点诊断=radio 有/没有 → diagnoseNode → 结论 alert+加批归因表+分支横评表+entrust 表+兜底 rawTasks。
- instance/index.vue [更多]加[流程诊断](Search 图标); api/workflow/index.js 加 getDiagnosisInit/diagnoseNode。
- 待联调验证: 真实流程数据跑一遍 diagnosisInit/diagnoseNode; FormRender 预填的 fieldPerm 副作用观察。

## 流程诊断 4.3 人员维度补充实现(2026-08-17 晚第二轮 grill)

第二轮 grill 澄清 4.3 语义: **人员维度与节点维度完全独立判断, 前提=节点存在**(present=true 才启用), 交互=单选一个人+radio(此审批人存在/不存在) → 判定此人 ∈ 该节点实际审批人 + 与预期对账 + 归因。全部落地:

**后端**:
- `NodeDiagnosisVo` +`expectedApprovers/actualApprovers/ruleDesc/personDiagnosis`(ApproverVo{userId,name,mark,source,time}; PersonDiagnosis{presentPerson,expectationMismatch,message,inference,inferenceNote}); EntrustRecordVo +originalId/actualId。
- `NodeDiagnosisRequestVo` +`personId/expectedPersonPresent`。
- `ProcessDiagnosisBizServiceImpl`: present 分支计算应审人/实际审批人; 带 personId 时返回 personDiagnosis。
  - **应审人复用 preview 链路**: `bpmnConfBizService.taskPagePreviewNode(params)`(params 不带 processNumber 走 getPreviewNode 当前值路径; LF→lfFields=formValues, DIY→lfConditions=条件字段值), 取目标节点 property.emplList 后**自行镜像 reTreatNodeAssignee 语义**应用 entrust 标记(name 后缀 +加签/-减签/*转办)。
  - **归因证据优先级**: 有=加签引入>转办引入>配置命中(inConfig: expected 中无 mark)>应审人命中>推断(动态评估差异); 无=减签移除>转办给他人>配置不含>推断。inference=true 时前端黄色提示"含推断成分"。
- 关键坑: preview 不带 processNumber 时 entrust 不会自动应用(只在带 processNumber 的 taskPagePreviewNode 分支里调 reTreatNodeAssignee), 所以必须自己应用 entrust 标记。

**前端** diagnosisDialog.vue: 节点诊断 tab 新增"审批人诊断"section(节点 present 才显示): 应审人/实际审批人对照表(mark 着色)+ 候选 chips(应审人∪实际∪entrust, 名称去标记后缀)+ selectUserDialog 选任意人 + radio + 诊断按钮 → 复用 diagnoseNode(personId 参数) 一次请求, 结果 alert 展示(一致/相反/含推断)。
- 全量编译通过(base+engine install -Dgpg.skip)。
- 待联调: preview 链路(应审人评估)在真实流程上的表现, 尤其 自选(7)/表单相关(16)节点 emplList 是否为空。

## 流程诊断需求 grill 会话决策记录(2026-08-17 晚)

需求: 流程管理-流程监控 条目[更多]里加[流程诊断]。经 grill-me 会话逐分支确认, 全部决策如下:

- **A 页面形态**: 全屏 dialog(94% 宽, 参照版本比较), 不加独立路由/菜单。
- **B 流程图组件**: 从 `versionCompare/compareTree.vue+cmpNode.vue` 抽公共只读流程树到 `components/Workflow/FlowTree/`, 高亮状态从写死 `__diff` 泛化为通用 `__mark`; 版本比较改引用新位置。关键事实: compareTree 渲染的树就是 bpmnConf detail `confConfigJson.nodeConfig` 原始树, cmpNode select 事件抛完整 node 对象(含 id/nodeId/conditionList)。
- **C 接口三层**: ① 新增 `GET /bpmnConf/diagnosisInit?processNumber=` (bpm_business_process.version=bpmnCode → t_bpmn_conf.id, 返回 confId/bpmnCode/发起人/表单数据); ② 改造 `getBpmVerifyInfoVos`: BpmVerifyInfoVo 加 `nodeId` 字段, 历史记录 join af_hi_taskinst(bpm_verify_info.task_id=ID_) 回填, 运行中任务 findTaskInfor 加 select `t.NODE_ID`; node_id null 时 log error, 发起人虚拟节点豁免; ③ 新增 `POST /bpmnConf/diagnoseNode`(processNumber+nodeId+expectation) 按需诊断。
- **D 判定逻辑**: 后端算结论(条件求值走 FormFactory 适配器+复用引擎条件判定), 前端只做交互渲染+node_id 集合比对。
- **E 条件诊断深度**: 包含目标节点的网关全分支横评(条件|实际值|命中), 实际值取**当前**业务表单值(不做历史快照回放, 界面加提示: 表单审批中被改过则与当时判断可能不符)。
- **F 调试区**: flowDebug/index.vue 主体抽成 `components/Workflow/FlowDebug/debugPanel.vue`(props: confId/initiator 只读/prefillFormValues), 原页面变薄壳; 诊断里发起人固定为 bpm_business_process.create_user; **预填真实表单值**支持 what-if。
- **G 归因优先级矩阵**(短路顺序): ①node_id 在 af_hi_taskinst+af_ru_task 任一条=存在过(重提多次也算) ②尚未到达(流程未结束+拓扑位置在当前停留节点之后, 排在条件求值之前防伪结论) ③条件分支横评 ④减签跳过(entrust actionType=3, 表自带 nodeId 无需 join) ⑤加批归因(上节点按钮含加批 buttonType=19 + bpm_verify_info verifyStatus=9 + variableConfigJson signUps) ⑥兜底裸列原始数据明说无法归因。用户期望与事实矛盾时以事实为准并明示。
- **布局**: 版本比较式三栏固定区域(流程图|审批路径|诊断面板), 审批路径与图联动高亮, node_id null 条目(发起人外)标灰+console.error。
- 关键事实: entrust actionType 0/1=转办 2=加签 3=减签 4=表单关联刷新(BpmnConfBizServiceImpl 2584-2596 有现成语义代码); 加批 buttonType=19(addApproval)。
- 待实施: 后端 3 接口 + FlowTree 抽取 + 诊断 dialog 三栏 + debugPanel 抽取。
